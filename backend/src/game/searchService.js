async function searchAnime(query, { limit = 10 } = {}) {
    // Step 3 skeleton: this is intentionally a placeholder until Jikan proxy is implemented.
    return {
        query,
        source: 'placeholder',
        results: [],
        limit
    };
}

module.exports = {
    searchAnime
};
